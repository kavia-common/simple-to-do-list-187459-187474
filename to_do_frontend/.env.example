# Frontend environment configuration for local development

# PUBLIC_INTERFACE
# REACT_APP_API_BASE is the base URL for the backend API that the frontend calls.
# For local development, point this to the backend's /api base path.
# Example backend default: http://localhost:4000
REACT_APP_API_BASE=http://localhost:4000/api

# Optional fallbacks/overrides used in code for flexibility across environments.
# If REACT_APP_API_BASE is not set, the app will also check REACT_APP_BACKEND_URL.
# Typically you do NOT need to set these for local dev.
# REACT_APP_BACKEND_URL=

# Other optional variables (not strictly required for local dev):
# REACT_APP_FRONTEND_URL=http://localhost:3000
# REACT_APP_WS_URL=
# REACT_APP_NODE_ENV=development
# REACT_APP_NEXT_TELEMETRY_DISABLED=1
# REACT_APP_ENABLE_SOURCE_MAPS=true
# REACT_APP_PORT=3000
# REACT_APP_TRUST_PROXY=false
# REACT_APP_LOG_LEVEL=info
# REACT_APP_HEALTHCHECK_PATH=/health
# REACT_APP_FEATURE_FLAGS=
# REACT_APP_EXPERIMENTS_ENABLED=false
