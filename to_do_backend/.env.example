# Backend environment configuration for local development

# PUBLIC_INTERFACE
# PORT controls which port the Express server listens on.
PORT=4000

# PUBLIC_INTERFACE
# FRONTEND_URL sets the allowed CORS origin.
# For local development with the React app, this is typically:
FRONTEND_URL=http://localhost:3000

# Optional healthcheck path (defaults to /health if not set)
# HEALTHCHECK_PATH=/health
